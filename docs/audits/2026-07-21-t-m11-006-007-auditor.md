# audit_result — Anvil T-M11-006/007 (Regime B) — VERDICT: pass

- Target: `eb2b608` · Directive `dir-20260721-anvil-m11-006-007` (builder ≠ director ≠ auditor
  verified). Independent persona; read-only; own scratch smoke.
- Both rows genuinely Met: loader branch byte-parity with the fps2/net idiom
  (loadModules.ts:59-61); genre-arpg package + games/ untouched; starter matches the v2
  T-M10-009 shape with fixture-traceable content — all three disclosed builder choices judged
  fixture-faithful NECESSITY (speed:50 is schema-required + fixture-asserted; trait files are
  minimal decompositions of the fixture prefab; core-only smoke follows S-ARPG §6 inert-marker
  design). Marker test strengthened additively, nothing weakened. Docs flips consistent
  (deliberate failures 1→0). Executes-clean RE-RUN independently: full pnpm test exit 0, cli
  9/9 incl. the ARPG marker; off-suite all green; own scratch scaffold validate/test/describe/
  capabilities all correct. **First fully-green suite state confirmed.**
- **W1 (warning):** lockfile NOT strictly additive — uWebSockets.js resolution rewritten
  tarball→git+ssh in 3 places (same pin 442087c0, no version drift; pnpm re-normalization; the
  exact rewrite the T-M10-008 builder reverted). CI cold-install fetch risk; undisclosed in the
  commit message. → FIX IN THE TAIL CHUNK (restore tarball form).
- **W2 (warning/P-item):** core validate.ts:82 MODULE_UNKNOWN hint omits genre-arpg
  (pre-existing staleness, correctly fenced out). → tail chunk.
- Standing: gravewake tyrant_edge.json critMult 0.35 vs min(1) — pre-existing, tracked under
  T-M11-009 disposition in the tail.
