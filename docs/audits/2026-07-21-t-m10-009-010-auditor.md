# audit_result — Anvil T-M10-009/010 (Regime B) — VERDICT: pass

- Target: `8fe56ca` · Directive `dir-20260721-anvil-m10-009-010` attempt 2 (builder ≠ director ≠
  auditor — verified). Independent fresh-context persona; read-only; scratch smoke.
- **Crux upheld:** changing the deliberate marker's `requirements: 2` → `3` is SPEC-GROUNDED
  reconciliation, not test-weakening — S-SCHEMA:58 fixes no count ("at least one"); S-AUTHORING
  §4 is illustrative (shows 1); the shipped migrate baseline emits 3 (migrate.ts:55-58); the
  sibling migrated-project test asserts 3; all 12 shipped intents carry 3. Only the placeholder
  count changed; every other assertion stands; rationale inline in the test.
- T-M10-009 Met (none-path dogfoods migrateProject; genre path personalizes; scratch smoke:
  fresh scaffold validates, describes schemaVersion:2/requirements:3). T-M10-010 Met (all 12
  diffs game.yaml-only; intents never overwritten per S-AUTHORING §2 — verified via ls-tree;
  idempotence live-verified). Docs flips consistent (deliberate count 2→1; only T-M11-007
  remains). Executes-clean re-run reproduced exactly: cli 9 tests/1 failed = the ARPG marker;
  009 marker GREEN; everything previously green still green; evidence file matches.
- Findings: info — genre-path duplicates the migrate intent-summary boilerplate string
  (drift-prone if migrate.ts wording changes; design-forced). warning (ledger, pre-existing,
  unrelated) — games/gravewake tyrant_edge.json critMult 0.35 vs schema min 1 (schema
  contentSchemas.ts:15; last touched 8c4ee0e); needs its own disposition.
