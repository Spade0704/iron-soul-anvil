# audit_result — Anvil T-M10-008 CLI wiring (Regime B) — VERDICT: pass

- Target: commit `2172841` · Directive `dir-20260721-anvil-m10-008` (builder
  `lattice:iron-soul-anvil/cli-builder-01` ≠ director — verified) · Independent fresh-context
  persona Auditor; read-only.
- All criteria Met, independently re-run: migrate/describe/capabilities are thin wrappers over
  the existing T-M10-005/006 library (`compileProject`/`migrateProject` imports; zero logic
  reimplemented); JSON shapes per S-CLI §2; migrate preview provably non-mutating (sha1-equal,
  test-asserted). Scope exactly 9 files (CLI pkg + docs + evidence); pnpm-lock additive-only
  47/0 with the uWebSockets.js tarball resolution byte-identical to parent (proxy-rewrite revert
  holds — no git+ssh regression). Suite re-run reproduces the documented state exactly: all
  previously-passing suites green; CLI 2 failed | 7 passed where both failures are the
  MUST-REMAIN-VISIBLE pending markers for T-M10-009 / T-M11-007 (pre-existing by design; this
  commit flipped the third — the in-scope migrate marker — green). Live smoke: preview →
  --write → describe --json → capabilities --json all correct incl. MIGRATION_REQUIRED hint
  path + exit-code conventions. New tests assert JSON shape / error codes / determinism /
  non-mutation, not bare exit-0.
- Findings (info ×3): failure-stream inconsistency (migrate stderr vs describe/capabilities
  stdout JSON — cosmetic); S-CLI §4 "Planned" section now carries landed-command prose
  (placement oddity); DFTS below-threshold disposition disclosed-not-skipped in the directive.
