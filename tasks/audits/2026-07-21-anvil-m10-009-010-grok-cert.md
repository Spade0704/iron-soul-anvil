---
date: 2026-07-26
slug: 2026-07-21-anvil-m10-009-010
target_repo: D:/Projects/IronSoul-Anvil/iron-soul-anvil
range: 8fe56caa8713a9aa7ec9046403f884046040fd8a
certifier: Grok (xAI) — EMCC External Certifier
verdict: PASS
chat: PASS
execute: deferred (CISO gate)
vision: n/a
---

# Grok cert — 2026-07-21-anvil-m10-009-010 (Anvil T-M10-009/010 schema-v2 scaffold + migrate examples/templates)

## 1. Disclosure

- **Cold read.** Certifier did not author the range. Producer `lattice` / builder
  `lattice:iron-soul-anvil/cli-builder-02`; commit author Claude (Anthropic).
- Handoff selected by `poll_select.py` (oldest local pending `certifier_id: grok`).
- Range is single commit `8fe56ca` on
  `origin/claude/herald-marketing-publish-ru4szs` (not first-parent of current
  `main`; M10+M11 later landed on `main` via squash `8254a1b`). Certification is
  of the **handoff-pinned range**, not of tip of main.
- Pre-gate: `validate_poll_handoff.py` + `validate_cert_handoff.py` → **PASS**.

## 2. Chat

### Scope (Stage 1)

`git diff --stat 8fe56ca^..8fe56ca` — 21 files, +480/−57:

| Path | Role |
|------|------|
| `anvil/packages/cli/src/index.ts` | `anvil new` → v2 via migrate dogfood; template intent summary personalize |
| `anvil/packages/cli/src/cli.integration.test.ts` | Marker: scaffold `requirements: 2` → `3` (reconciled) |
| `anvil/examples/*/game.yaml` (7) | schemaVersion 1→2 + intent pointer fields |
| `anvil/templates/*/game.yaml` (5) | same migration surface |
| `anvil/docs/design/specs/S-CLI.md` | Deliberate failures 2→1; v2 scaffold notes |
| `anvil/docs/design/specs/S-AUTHORING.md` | Status rows |
| `anvil/docs/design/11_RECIPES_AND_TEMPLATES.md` | v2 table |
| `anvil/docs/design/16_PLANNING_STATUS_AND_GAPS.md` | G-M10-CLI gaps closed |
| `anvil/docs/design/18_TESTING_AND_CI.md` | Counts |
| `anvil/docs/design/20_FULL_TASK_BREAKDOWN.md` | T-M10-009/010 → **[x]** |
| `docs/evidence/2026-07-21-t-m10-009-010-tests.txt` | Executes-clean evidence |

No scope breach: CLI scaffold path + example/template manifests + design docs + evidence.
No `game.spec.yaml` paths in the delta (intents not rewritten). No unrelated app/UI paths.

### Mechanical floor (Stage 2)

| Check | Result |
|-------|--------|
| `validate_poll_handoff.py` / cert-handoff schema | PASS |
| `evidence_ref` (`docs/evidence/2026-07-21-t-m10-009-010-tests.txt`) | Present, readable, documents post-change suite |
| Consumer `scripts/nonbuild_check.py` | Absent — skip |
| Consumer `scripts/doc_drift_check.py` | Absent — skip |
| Consumer `scripts/reconcile_backlog.py` | Absent — skip |
| Tree porcelain before cert write | empty (byte-clean) |

**Independent cold checks (certifier):**

- `migrate.ts` at range emits **three** baseline requirements:
  `lifecycle.start`, `input.responds`, `lifecycle.restart` (lines 54–57).
- `git diff --name-only` contains **zero** `game.spec.yaml` paths → T-M10-010 claim
  that intents were never overwritten is git-true.
- Sample intent `anvil/examples/hello-empty/game.spec.yaml` at `8fe56ca` carries the
  same three requirement ids as migrate default.
- Example/template `game.yaml` deltas only: `schemaVersion: 2`, `intent: game.spec.yaml`,
  plus migrate-added fields (`version`, `contentRoot`, `assetsRoot`).

**Evidence summary (Lattice run, 2026-07-21):**

- Lint exit 0.
- Root `pnpm test`: CLI **1 failed | 8 passed (9)** — sole failure is deliberate
  T-M11-007 ARPG scaffold marker; T-M10-009 scaffold/describe marker is GREEN with
  `schemaVersion: 2` and `requirements: 3`.
- Off-suite authoring + genre-arpg green; examples validate/test ok on v2.
- Smokes: fresh scaffold describe, migrate no-op on already-v2, template genre
  scaffold personalizes intent summary.
- Documented pre-existing out-of-scope: `games/gravewake` `tyrant_edge.json`
  `critMult 0.35` content violation (not this range).

### Proposal vs job (Stage 3)

Job: T-M10-009 make `anvil new` emit schema v2 + intent; T-M10-010 migrate active
examples/templates to v2 (S-CLI + S-AUTHORING + breakdown).

Delivered:

- **none-path scaffold:** still writes a v1 skeleton then **dogfoods**
  `migrateProject(base, { write: true })` so baseline intent is single-sourced from
  authoring migrate (not a second hard-coded scaffold copy).
- **template-genre path:** after personalizing `game.yaml` id/title, personalizes
  copied `game.spec.yaml` summary with the same migrate boilerplate wording.
- **12 manifests** (7 examples + 5 templates) migrated on `game.yaml` only.
- **Marker reconciliation:** integration test expectation `requirements: 2` → `3`
  with inline rationale; matches migrate default, sibling migrated-describe case,
  and shipped intents. Spec does not fix scaffold count at 2 (S-SCHEMA “at least one”;
  S-AUTHORING §4 illustrative). This is **not** silent test-weakening — only the
  incorrect placeholder count moved; other assertions remain.
- Docs: deliberate failure count 2→1 (only T-M11-007 remains); breakdown 009/010 [x].

No undercoverage against the stated job. ARPG scaffold correctly left failing (out of range).

### Substance (Stage 4)

- Scaffold does not reimplement migration; it calls shipped `migrateProject`.
- Intent non-overwrite is structural (absent from diff), not prose-only.
- requirements:3 change is grounded in migrate.ts + repo baseline consistency
  (fresh scaffold must describe like fresh migrate).
- Docs are real status/count updates, not stubs.
- Auditor PASS (info: genre-path summary string drift-prone; warning ledger
  gravewake pre-existing) independently consistent with this cold read; agreement
  cites own git evidence, not deferral to auditor.

**Chat token: PASS**

## 3. Execute

Handoff has no `execute_approved: true` and no CISO body approval for independent rebuild.
Per interim CISO Execute gate: **deferred (CISO gate)**.

Agreement with Lattice `evidence_ref` is therefore not re-run as a full suite in this
harness; Chat relies on committed evidence artifact + cold code/test/doc/git reads
above (including migrate.ts and intent non-touch proof).

**Execute token: deferred (CISO gate)**

## 4. Vision

Not a frontend/UI change set (CLI scaffold + manifests + docs). No comp path. **n/a**.

**Vision token: n/a**

## 5. Verdict

**PASS**

Director closes on dual-PASS (Auditor + Grok). Certifier does not merge.
