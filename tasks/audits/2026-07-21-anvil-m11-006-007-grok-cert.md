---
date: 2026-07-26
slug: 2026-07-21-anvil-m11-006-007
target_repo: D:/Projects/IronSoul-Anvil/iron-soul-anvil
range: eb2b6084f84d648ebf62c9096b5ad39760e187f9
certifier: Grok (xAI) — EMCC External Certifier
verdict: PASS
chat: PASS
execute: deferred (CISO gate)
vision: n/a
---

# Grok cert — 2026-07-21-anvil-m11-006-007 (Anvil T-M11-006/007 genre-arpg loader + v2 ARPG starter)

## 1. Disclosure

- **Cold read.** Certifier did not author the range. Producer `lattice` / builder
  `lattice:iron-soul-anvil/cli-builder-03`; commit author Claude (Anthropic).
- Handoff selected by `poll_select.py` (oldest local pending `certifier_id: grok`
  after concurrent race-close of m10-009-010).
- Range is single commit `eb2b608` on
  `origin/claude/herald-marketing-publish-ru4szs` (not first-parent of current
  `main`; M10+M11 later landed on `main` via squash `8254a1b`). Certification is
  of the **handoff-pinned range**, not of tip of main.
- Pre-gate: `validate_poll_handoff.py` + `validate_cert_handoff.py` → **PASS**.

## 2. Chat

### Scope (Stage 1)

`git diff --stat eb2b608^..eb2b608` — 21 files, +499/−44:

| Path | Role |
|------|------|
| `anvil/packages/cli/src/loadModules.ts` | Register `genre-arpg` → `@anvil/genre-arpg` / `arpgModule` |
| `anvil/packages/cli/src/index.ts` | `new --genre arpg` + help + template map → `arpg-starter` |
| `anvil/packages/cli/package.json` | workspace dep `@anvil/genre-arpg` |
| `anvil/packages/cli/src/cli.integration.test.ts` | ARPG marker strengthened (schema v2, intent file, capabilities) |
| `anvil/templates/arpg-starter/**` | New schema-v2 starter (manifest, intent, content, smoke) |
| `anvil/pnpm-lock.yaml` | Importer link for genre-arpg (+ incidental uWS resolution rewrite) |
| Specs / design docs (S-CLI, S-ARPG, S-AUTHORING, 18, 20) | Status/count flips; T-M11-006/007 **[x]** |
| `docs/evidence/2026-07-21-t-m11-006-007-tests.txt` | Executes-clean evidence |

No scope breach: CLI loader + starter template + marker + docs + evidence.
**Zero** `games/` paths in the delta (gravewake content out of range). T-M11-008/009
left open on breakdown (**[ ]**).

### Mechanical floor (Stage 2)

| Check | Result |
|-------|--------|
| `validate_poll_handoff.py` / cert-handoff schema | PASS |
| `evidence_ref` (`docs/evidence/2026-07-21-t-m11-006-007-tests.txt`) | Present, readable, documents post-change suite |
| Consumer `scripts/nonbuild_check.py` | Absent — skip |
| Consumer `scripts/doc_drift_check.py` | Absent — skip |
| Consumer `scripts/reconcile_backlog.py` | Absent — skip |
| Tree porcelain before cert write | empty (byte-clean) |

**Independent cold checks (certifier):**

- Loader branch is byte-parity with the fps2/net idiom:
  `else if (id === "genre-arpg") { import("@anvil/genre-arpg"); out.push(m.arpgModule); }`.
- Package export at range: `export { arpgModule } from "./module.js"` — name matches loader.
- Starter `game.yaml`: `schemaVersion: 2`, `intent: game.spec.yaml`, `modules: [genre-arpg]`.
- Starter intent carries the three-requirement baseline
  (`lifecycle.start`, `input.responds`, `lifecycle.restart`) consistent with M10 migrate default.
- Marker test is **strengthened additively** (schemaVersion:2, game.spec.yaml exists,
  capabilities ids `authoring-v2`/`core`/`genre-arpg`); prior asserts retained.
- No `games/` path in `git diff --name-only` → gravewake `tyrant_edge` remains out-of-scope.

**Evidence summary (Lattice run, 2026-07-21):**

- Lint exit 0.
- Root `pnpm test` exit 0; CLI **9/9** including ARPG scaffold marker.
- Off-suite authoring + genre-arpg green.
- Smoke: `anvil new --genre arpg` → validate/test/describe/capabilities ok; describe
  shows `schemaVersion: 2` and capability ids matching the marker.
- Aggregate `pnpm check` still stops at pre-existing `validate:gravewake`
  (`critMult 0.35` < min 1) — title content, not this range; tracked T-M11-009.

**Lockfile note (Auditor W1, non-blocking for this range job):** three uWebSockets.js
resolution lines rewritten tarball→git+ssh at same pin `442087c0` (pnpm re-normalization).
Same class of rewrite previously reverted on T-M10-008. Routed to tail chunk; does not
undo loader/starter delivery. Disclose only — not a Chat FAIL for T-M11-006/007 scope.

### Proposal vs job (Stage 3)

Job: T-M11-006 register `genre-arpg` in generic CLI loader; T-M11-007 schema-v2
`anvil new --genre arpg` starter (S-CLI + S-ARPG + breakdown).

Delivered:

- **Loader:** explicit import branch for `genre-arpg` + workspace dep (T-M11-006).
- **Starter:** `templates/arpg-starter` with v2 manifest/intent, minimal declarative
  content (traits/prefab/actor/area/progression), smoke test (T-M11-007).
- **CLI surface:** help text + supported-genre list + template map include `arpg`.
- **Marker:** last deliberate CLI failure flipped green and strengthened with
  capabilities assertion.
- **Docs:** deliberate failures 1→0; breakdown 006/007 **[x]**; 008/009 untouched.

No undercoverage against the stated job. Root-suite inclusion of genre-arpg package
tests correctly left for T-M11-008.

### Substance (Stage 4)

- Loader is real import wiring, not a stub comment.
- Starter content is minimal but structural (ids, traits, prefab components, smoke
  asserts) — matches auditor “fixture-faithful NECESSITY” judgment; own read agrees
  content is non-hollow.
- Marker strengthen is additive, not silent weakening.
- Docs are real status/count updates, not stubs.
- Auditor PASS (W1 lockfile; W2 MODULE_UNKNOWN hint stale; gravewake standing) is
  independently consistent with this cold read; agreement cites own git evidence,
  not deferral to auditor.

**Chat token: PASS**

## 3. Execute

Handoff has no `execute_approved: true` and no CISO body approval for independent rebuild.
Per interim CISO Execute gate: **deferred (CISO gate)**.

Agreement with Lattice `evidence_ref` is therefore not re-run as a full suite in this
harness; Chat relies on committed evidence artifact + cold code/test/doc/git reads
above (loader parity, export name, starter shape, marker strengthen, zero games/ touch).

**Execute token: deferred (CISO gate)**

## 4. Vision

Not a frontend/UI change set (CLI loader + starter template + docs). No comp path. **n/a**.

**Vision token: n/a**

## 5. Verdict

**PASS**

Director closes on dual-PASS (Auditor + Grok). Certifier does not merge.
